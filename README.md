# HOW TO START

To display the bug, nginx is required, and it is necessary to use the configuration located in the root of the repository: nginx.conf.

Install deps:

> npm run ci

After run nginx:

> npm run dev

After run go to [http://localhost:1000/](http://localhost:1000/)
